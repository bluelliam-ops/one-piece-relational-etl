# Copy this file to .env and fill in your own Postgres connection string.
# The notebook loads this with python-dotenv instead of hardcoding credentials.
#
# Example (Aiven, Supabase, RDS, local Postgres, etc. all work — any standard
# libpq connection URI):
DATABASE_URL=postgresql://user:password@host:port/dbname?sslmode=require
